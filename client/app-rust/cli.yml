name: aoe2overlay
version: "0.1"
author: "simonsan <simon@systemli.org>"
about: "Pulls our overlay from the web and shows it on screen"
args:
    - config:
        short: i
        long: steamid
        value_name: steam_id
        help: "Sets the Steam-ID you want to generate overlays for"
        required: true
        takes_value: true
